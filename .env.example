# Front-end function URL (Supabase Edge Function)
VITE_MOMENTUM_FUNCTION_URL=https://<project>.functions.supabase.co/momentum

# Supabase client (public) keys for auth & optional DB access from client
VITE_SUPABASE_URL=https://<project>.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key

# NOTE: Do NOT add server-only secrets (OPENAI_KEY) here. Keep OPENAI_KEY in Supabase function secrets.
